name: BETA VM Deployment 

on:
  workflow_dispatch:
  push:
    branches:
      - main

env:
  HOST: cpc-beta.ukwest.cloudapp.azure.com
  USERNAME: ubuntu
  PORT: 22
    
jobs:
  build:
    runs-on: ubuntu-latest
    name: Build
    env:
      CI: false
    steps:
      - uses: actions/checkout@v3

      - name: Use Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 16.20.2
          cache: 'npm'

      - run: npm install
      - run: npm run build
        env: 
          CI: false

      - name: Verify build output
        run: |
          echo "Build directory contents:"
          ls -la build/
          [ -f build/index.html ] || { echo "Error: index.html not found in build directory"; exit 1; }

      - name: 'Upload Artifact'
        uses: actions/upload-artifact@v4
        with:
          name: build-artifact
          path: build/
          retention-days: 2

  Deployment:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Download artifact
        uses: actions/download-artifact@v4
        with:
          name: build-artifact
          path: downloaded-build/

      - name: Verify downloaded files
        run: |
          echo "Downloaded artifact contents:"
          ls -la downloaded-build/
          [ -f downloaded-build/index.html ] || { echo "Error: index.html not found in downloaded artifact"; exit 1; }

      - name: Prepare remote directory
        uses: appleboy/ssh-action@v0.1.7
        with:
          host: ${{ env.HOST }}
          username: ${{ env.USERNAME }}
          key: ${{ secrets.NEW_SSH_KEY }}
          port: ${{ env.PORT }}
          script: |
            sudo rm -rf /var/www/html/*
            sudo mkdir -p /var/www/html
            sudo chown -R ${{ env.USERNAME }}:${{ env.USERNAME }} /var/www/html
            sudo chmod -R 755 /var/www/html

      - name: Deploy files
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ env.HOST }}
          username: ${{ env.USERNAME }}
          key: ${{ secrets.NEW_SSH_KEY }}
          port: ${{ env.PORT }}
          source: "downloaded-build/*"
          target: /var/www/html/
          overwrite: true
          strip_components: 0

      - name: Verify deployment
        uses: appleboy/ssh-action@v0.1.7
        with:
          host: ${{ env.HOST }}
          username: ${{ env.USERNAME }}
          key: ${{ secrets.NEW_SSH_KEY }}
          port: ${{ env.PORT }}
          script: |
            echo "Deployed files:"
            ls -la /var/www/html/
            [ -f /var/www/html/index.html ] || { echo "Error: index.html not deployed"; exit 1; }
            sudo chown -R www-data:www-data /var/www/html
            sudo chmod -R 755 /var/www/html
            sudo systemctl restart nginx
